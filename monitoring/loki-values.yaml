loki:
  enabled: true
  isDefault: false # Only needed if you have other log sources
  persistence:
    enabled: true
    storageClassName: longhorn
    size: 20Gi
    accessModes:
      - ReadWriteOnce

promtail:
  enabled: true