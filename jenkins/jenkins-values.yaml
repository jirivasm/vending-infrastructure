controller:
  # Moves the controller to the Raspberry Pi
  nodeSelector:
    kubernetes.io/arch: "arm64"
  resources:
    requests:
      cpu: "50m"
      memory: "256Mi"
    limits:
      cpu: "1000m"
      memory: "512Mi"
  image:
    # Use the official multi-arch Jenkins image
    repository: "jenkins/jenkins"
    tag: "lts"
    pullPolicy: Always
  overwritePlugins: true
  installPlugins:
    - kubernetes
    - workflow-aggregator
    - git
    - docker-workflow
    - docker-commons
    - pipeline-stage-view
  # CRITICAL: Keep your Java limits to protect the Pi's RAM
  javaOpts: "-Xmx384m -Xms256m -Dhudson.security.csrf.DefaultCrumbIssuer.EXCLUDE_SESSION_ID=true"
  jenkinsOpts: "--prefix=/" 
  persistence:
    enabled: true
    existingClaim: "jenkins-pvc" # Longhorn will move this to the Pi
  serviceType: ClusterIP

agent:
  enabled: true
  # Use the official agent image (Kaniko handles the builds)
  image: "jenkins/inbound-agent" 
  tag: "latest"
  imagePullPolicy: Always
  # Force the build workers to the Pi
  nodeSelector:
    kubernetes.io/arch: "arm64"
  resources:
    requests:
      cpu: "500m"
      memory: "1Gi"
    limits:
      cpu: "1000m"
      memory: "2Gi"